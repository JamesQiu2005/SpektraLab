## Do not rewrite three dot click second layer button, use system-provided one only

## for the size three dot, include set to originals

## Open with allows the user to select the default open_app

## Only the chosen image gets a white frame around it, and the actual image name got displayed, the non-selected ones are just there (no actual grey frame around it)

## Multi-select is allowed, and the export setting is applied to all the selected images. Under Viewer Mode (the reference layout) the viewed image stays as the one the user is previously on

## There are two modes, Grid (the icon used is square.grid.2x2 in swift) and Viewer (the icon used is rectangle.grid.3x1, rotated 90 degrees) Remember to write the transition between the modes in both ways

## Four Naming options, Original Name, Film, Print, Date are provided and user can only choose between these four (at least one must be selected), selection is marked with Orange highlight #f08724, and the actual order can be altered by use dragging and rotating each of them

## the export page used roughly the same element as the one in the main UI, so reuse whatever you get to reuse

## The base we're in is 3000X2000 (canvas size), run visual check to ensure that you don't fuck up the layout